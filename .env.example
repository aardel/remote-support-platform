# Server Configuration
NODE_ENV=development
PORT=3000
SERVER_URL=http://localhost:3000

# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=remote_support
DB_USER=remote_support_user
DB_PASSWORD=change_me

# Redis (optional)
REDIS_HOST=localhost
REDIS_PORT=6379

# Session
SESSION_SECRET=change_me_to_random_string
JWT_SECRET=change_me_to_random_string
# When helper requests a session and device has no technician, use this (optional; default: "system")
# DEFAULT_TECHNICIAN_ID=your-technician-uuid-or-nextcloud-id
BACKEND_URL=http://localhost:3000
NEXTCLOUD_URL=https://your-nextcloud-domain
NEXTCLOUD_CLIENT_ID=your_nextcloud_client_id
NEXTCLOUD_CLIENT_SECRET=your_nextcloud_client_secret

# VNC Bridge (optional — main path uses WebRTC + Electron helper; use VNC only if you need legacy/websockify)
VNC_LISTENER_PORT=5500
WEBSOCKET_PORT=6080

# File Upload
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=104857600

# SSL/TLS (for production)
# SSL_CERT_PATH=/etc/letsencrypt/live/your-domain.com/fullchain.pem
# SSL_KEY_PATH=/etc/letsencrypt/live/your-domain.com/privkey.pem
